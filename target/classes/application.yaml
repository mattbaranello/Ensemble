spring:
   datasource:
     username: ensemble
     password: ensemble
     url: jdbc:mysql://localhost:3306/ensemble
      
   jpa:
     hibernate: 
       ddl-auto: update
     show-sql: true
            
   sql:
     init:
       mode: always